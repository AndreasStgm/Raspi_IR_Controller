name: Validate Python code

on:
  workflow_dispatch:
  pull_request:
  # push:
  #   branches:
  #     - main
  
jobs:
  test:
    runs-on: ubuntu-24.04
    steps:
      - name: Placeholder
        shell: bash
        run: echo "great success!"
